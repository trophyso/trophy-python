# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .batch_metric_event_user import BatchMetricEventUser


class BatchMetricEvent(UniversalBaseModel):
    """
    A metric event submitted as part of a batch. Same shape as a single metric event, with the metric key included in the body.
    """

    key: str = pydantic.Field()
    """
    Unique reference of the metric as set when created.
    """

    user: BatchMetricEventUser = pydantic.Field()
    """
    The user that triggered the event.
    """

    value: float = pydantic.Field()
    """
    The value to add to the user's current total for the given metric.
    """

    attributes: typing.Optional[typing.Dict[str, str]] = pydantic.Field(default=None)
    """
    Event attributes as key-value pairs. Keys must match existing event attributes set up in the Trophy dashboard.
    """

    idempotency_key: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="idempotencyKey"),
        pydantic.Field(
            alias="idempotencyKey",
            description="Optional idempotency key for this event. When provided, the event is ignored if another event with the same idempotency key has already  been processed.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
